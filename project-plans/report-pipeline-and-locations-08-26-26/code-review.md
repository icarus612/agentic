# code-review

## Round 1 — 08-27-26

```
verdict: tentative
next: proceed
blocking: 0
non-blocking: 2
```

### Findings

- [non-blocking] **`docs/pipeline.md:66`'s `map` table row still asserts the unqualified "no git side effects" claim, which R2 was supposed to eliminate everywhere.** Verified: `git grep -n -i 'no git side effect\|zero git side effect' -- . ':!project-plans' ':!.artifacts'` returns exactly this one hit, nowhere else in the shipped tree. It is genuinely false as written: at `rigor >= med` a `ship: chat` run creates a detached snapshot worktree (`report.md` §D, `git worktree add --detach <scratch-dir>/snapshot HEAD` — confirmed at `orchestrators/skills/dae/report.md:31`), which is a real git side effect regardless of whether it touches the product branch. The claim is correctly *qualified* in three other places the plan named in its R2 risk ("no branch, no commit, nothing staged, nothing reaching the product branch — plus, at `rigor: low`, no worktree either"): `dae/SKILL.md:112` (verified), `docs/architecture.md:116` (verified), and this same file's own diagram a few lines above (`docs/pipeline.md` ~line 19-21, verified). This fourth occurrence, in the file's own Types table, was missed. It directly fails subphase 9.2's own acceptance criterion 8 ("No zero-side-effects claim in scope is left unqualified where 8.2 qualified it") — the lane-9 exit report's own verification for this criterion (`l9-exit.md:121`) only grepped `orchestrators/skills/dae/SKILL.md`, never `docs/pipeline.md` or `docs/architecture.md`, so the gap was never actually checked. Non-blocking because it's a doc-only phrasing residue with no behavioral impact (the file's own diagram states the correct, qualified version a few lines above it) — but it is exactly the same defect class the five prior rework rounds and the "exhaustive sweep" targeted, and it escaped all of them because the sweep's own vocabulary search (mode/confluence/transition) never covered this phrase. Fix: change `docs/pipeline.md:66`'s `map` row to match the qualified form used elsewhere, or point at `report.md`'s §D table rather than restating the claim (convention 11).

- [non-blocking] **`docs/architecture.md:147` mislabels `confluence-mode.md` as `(domain: confluence)`.** Verified: `orchestrators/skills/dae/confluence-mode.md` has no YAML frontmatter at all (confirmed by reading the file's first lines — it opens directly with `# confluence-mode — ...`, no `---` block, no `domain:` key), and it lives under `orchestrators/skills/dae/`, which per convention 4 is `domain: universal` (it syncs to `~/.claude/` via `sync-install.sh`, unlike genuine `domain: confluence` content). The tag `domain: confluence` is a real, meaningful frontmatter value elsewhere — confirmed present at `tool-based/confluence/skills/document-confluence/SKILL.md:4` — so this sentence conflates a universal dae middle file (Jira/requirements-capture prose, triggered by `CLAUDE_DOCS_PUBLISH`) with the actually-domain-scoped `document-confluence` skill. This is the item the caller flagged as known/found-by-the-sweep and asked to be judged rather than assumed pre-approved: judged here as real but non-blocking — it's a one-clause doc inaccuracy with no behavioral consequence, easily fixed by dropping the parenthetical or replacing it with a correct pointer to `document-confluence`'s actual domain tag.

### Verified and holding (spot-checked independently, not merely cited)

- **`ship` is genuinely locked, `analyze` genuinely always publishes.** `resolve-type.sh feature --ship chat` and `resolve-type.sh analyze --ship publish` both exit 1 with the locked-axis error naming the resolved type and its ship mode; `resolve-type.sh analyze` (bare) prints `SHIP=publish`; `map` is untouched (`EXPLORE=auto`, `RIGOR_EXPLORE=low`, `SHIP=chat`, no `branch:`). `workflows.yaml:23` matches.
- **The sweep's exhaustive claim holds up under independent re-verification**, including its own warning. Reproduced the `git grep -E` lookahead failure myself: it exits 128 with `fatal: -e option, '...': Invalid preceding regular expression` on stderr and empty stdout — a naive stdout-only check would read this as "clean." Re-ran with `-P` and got the one correct, non-residue hit (the module's own H1). Re-ran the AC1/AC3 patterns from `l13-exit.md` myself over the same scope and got identical results (empty in scope, real hits in the `project-plans/` positive control). Independently confirmed `document-local` is the only dispatched record stage anywhere (`document-confluence` appears only as "manual/recovery sync," never as a record/run stage, in every one of its 8 remaining mentions outside `tool-based/confluence/` and `project-plans/`), and ticket transitions are consistently attributed to `cleanup-merged` in `sync.md`, `diagnose.md`, `plan-reconcile.md`, `cleanup-merged/SKILL.md`, and `agent-agnostic/AGENTS.md` — `document-local/SKILL.md` has zero mentions of `transition` or `jira`.
- **Symlink direction verified on disk**, not just cited: `.github/workflows/publish-docs.yml` is git mode `100644` (real file), `scripts/publish-docs.yml` is mode `120000` pointing to `../.github/workflows/publish-docs.yml`. `find tool-based -type l` is empty.
- **`CLAUDE_DOCS_DIR`'s hard error works**, tested directly: a Confluence URL in a fixture `settings.json` produces `resolve-config: CLAUDE_DOCS_DIR must be a local path; a Confluence location belongs in CLAUDE_DOCS_PUBLISH (got: '...')` and exit 1; a normal local path still resolves and prints its source on stderr as before.
- **§E (run-dir protection) genuinely was not built**: `git diff main --stat -- orchestrators/hooks/` shows no worktree/cleanup/guard-related script touched beyond what the plan's other phases already account for.
- **Ask-vs-plan gate exists and was dogfooded on this run's own plan gate**: `review-plan/SKILL.md` step 7 implements the diff-against-the-ask discipline described in the ask's own §H; `plan-review.md` Round 2 "Priority 1" shows it was actually walked clause-by-clause against `the-ask.md` §A–H as amended, and correctly identified the pre-existing "no git side effects" overclaim as a Round 1 non-blocking note (which is the same defect class as this review's first finding above — it was seen and scoped to 8.2/9.2's acceptance criteria, but the fix didn't fully land, see above).
- **All 10 test suites pass** (`bash tests/*.test.sh`, individually, all green), and `bash -n` is clean across every changed script (`orchestrators/`, `agent-agnostic/`, `tests/`). `plan-lifecycle.sh check` fails only on the known, unrelated stray-zip file — not attributable to this plan.
- **F1–F5 all independently verified in the shipped code/docs**, not just read as claimed: F1 (`report.md`'s `--against: forbid` no longer keyed on row count), F2 (`plan-lifecycle.sh`'s `remove_empty_dir` tolerates an already-gone dir; regression cases 36/37 pass), F3 (7.2's oracle honestly matches its file scope — `tests/validate-plan.test.sh` exists and passes, 24 cases), F4 (`resolve-scratch.sh` has a real committed suite, `tests/resolve-scratch.test.sh`, 23 cases), F5 (`docs/architecture.md:64` and `docs/tool-based.md` both list `medusa`; `architecture.md:143`'s "six placeholder stubs" sentence is untouched, correctly).
- **DEEP explore's boundary and precedence rule both shipped**: per-ecosystem exclusion floor with "tracked beats baseline" as the discovery rule (`explore/SKILL.md:38-47`), and "where docs and source disagree, the source wins" (`explore/SKILL.md:36`).
- **`--kind` is gone from `resolve-config.sh`** (replaced by `--expect path|publish-target`, tested); the only surviving `--kind` tokens in the tree belong to the unrelated `validate-report.sh --kind exit|review` flag, which the ask never touched.

### Known, not reported as new (per caller's brief)

- The stray tracked zip failing `plan-lifecycle.sh check` — orchestrator error, removal is with the user.

### Open questions

(none — every item above was independently verified against the live tree, actual command output, or a reproduced test; nothing here requires a user decision)
