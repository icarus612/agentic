# report-pipeline-and-locations-08-26-26.plan-review

## Round 1 — 08-26-26

```
verdict: ready
next: proceed
blocking: 0
non-blocking: 2
```

### Findings

- [non-blocking] The compact run-shape diagrams (`orchestrators/AGENTS.md:45`, `docs/pipeline.md:22-25`, both edited by 6.2) will still read "zero gates, zero git side effects, no worktree, no PR" for the chat branch after this run's fix. That is now scoped to `map` alone (analyze moves to the publish branch) and is literally true at `rigor: low`, but at `rigor >= med` a `map` run can create the §D detached-snapshot worktree — a nuance the diagrams' compact ASCII art has no room to carry and that 4.1 explicitly assigns to `report.md` as the owning file. 6.2's acceptance criterion 8 ("no zero-side-effects claim in scope is left unqualified where 4.1 qualified it") technically covers this, so it is not a plan defect, only a spot worth the builder's attention when 6.2 is dispatched — confirm the diagrams read as "the common case" rather than an absolute, or add a one-clause footnote pointing at `report.md`'s §D table.
- [non-blocking] `CLAUDE_SCRATCH_DIR` (2.1) is a reasonable, convention-matching name — it follows the existing `CLAUDE_DOCS_DIR` / `CLAUDE_PROJECT_PLANS_DIR` / `CLAUDE_WORKFLOWS_DIR` / `CLAUDE_BASE_BRANCH` pattern and grepping the tree turns up no collision — but the planner's own A1 correctly flags that whether it collides with something Claude Code itself defines is outside this repo and was not verified. Worth a quick check (e.g. Claude Code's own env-var docs) before or during 2.1's build, though there is no realistic alternative name this planner should have picked instead.

### Open questions

(none — both items above are advisory, not blocking, and the plan's own A1 already surfaces the `CLAUDE_SCRATCH_DIR` uncertainty as an assumption for the record)

## Round 2 — 08-26-26

```
verdict: tentative
next: proceed
blocking: 0
non-blocking: 8
```

### Structural check

`validate-plan.sh project-plans/proposals/report-pipeline-and-locations-08-26-26.md` — `OK: plan is structurally valid (26 subphases)` (plus its standing `INFO: lane file-scope disjointness is not machine-checked` note). `plan-lifecycle.sh check --plans-dir project-plans` — `FAIL: unrecognized filename in proposals/: drive-download-20260824T043851Z-1-001.zip`, exit 1. Per the caller's brief this is the known, unrelated stray-zip failure (a file this run's own filename is unaffected by) and is not attributed to this plan. No other `FAIL:` lines from either script.

### Priority 1 — ask-vs-plan, done manually

Walked every clause of `the-ask.md` §A–H against the plan, as amended by the five confirmed post-ask decisions (local-primary docs, CI-triggered Confluence publish with in-run dispatch removed, `CLAUDE_DOCS_DIR` hard-erroring on a URL/scheme value, the symlink inversion, and the dropped read seam / dead `--kind`). Every ask item traces to an owning subphase or an explicitly-recorded, gate-approved divergence (DP-3's archived-file exclusion) or a legitimately dissolved item (F6, whose triggering scenario — `ship: publish` with a Confluence docs target — can no longer occur once `CLAUDE_DOCS_DIR` is hard-enforced local-only). No place found where the plan does something other than the ask-as-amended requires, and no required item found omitted. Confirmed specifically: `--ship` locked (§A1, phase 1), `map` unchanged/`explore: auto` preserved (§B), the exact §C3 three-rung resolution order implemented verbatim in 2.1, the §D ship/rigor worktree-need table reproduced verbatim in 8.2(d), §E left untouched (no recovery machinery), F1–F5 each mapped to an owning subphase, F6 dissolved with reasoning rather than silently dropped, §G's ask-vs-plan check built as Phase 7, and no lingering "read seam" or `--kind` implementation anywhere (every mention of either term in the plan is in the context of stating it was removed).

### Priority 2 — the symlink inversion (DP-8), verified independently

Reproduced both halves myself rather than trusting the citation:
- **Git mechanism** — built a throwaway repo with `.github/workflows/publish-docs.yml` as a real file and `scripts/publish-docs.yml` as a symlink to it (the plan's actual final direction). `git ls-files -s` showed `100644` for the real file and `120000` for the symlink; `cat scripts/publish-docs.yml` (through the symlink) printed the real YAML correctly. I also reproduced the *requested* (broken) direction — symlink under `.github/workflows/`, real file in `scripts/` — and confirmed `git cat-file -p` on that symlink's blob prints exactly the literal target-path string `../../scripts/publish-docs.yml`, not YAML, matching the plan's claim precisely.
- **GitHub's behavior** — fetched `github.com/orgs/community/discussions/109744` directly: confirmed it is an open, unresolved feature request; GitHub does not currently resolve symlinks in `.github/workflows`, and a symlink there fails CI immediately, including a reported attempt to symlink the whole `.github` folder.

Both halves of DP-8 are VERIFIED, not merely cited. The inverted direction (canonical real file in `.github/workflows/`, symlink in `scripts/`) is the only one that works, and it preserves every property the user asked for (tracked, viewable via `cat`, copyable, single real file) — confirmed by the `cat`-through-symlink test above.

### Priority 3 — subphase 2.3 as hub

Read in full against the live `agent-agnostic/rules/artifact-locations.md`. The "seven kinds, seven locations, six through the chain" recount is arithmetically consistent: Docs, Plans, Workflows, Run dir, Base branch (5, today) + Scratch dir + `CLAUDE_DOCS_PUBLISH` (2 new) = 7 kinds; of those, Docs/Plans/Workflows/Base-branch (4, today) + Scratch dir (rung 1 via `resolve-config.sh`) + `CLAUDE_DOCS_PUBLISH` (via the same chain) = 6 through the chain, leaving the run dir as the sole non-chain exception exactly as today. The self-contradiction the caller flagged ("six kinds" vs "seven kinds" in different paragraphs) is **not present** in the plan as it stands now — both occurrences at lines 355 and 377 already read "seven kinds, seven locations, six through the chain" consistently; whatever inconsistency existed was already fixed in the planner's own sweep before this file reached me. The `artifact-locations.md:9-13` citation for the current shape-polymorphic Docs bullet is accurate to the live file (verified line-by-line). One real gap found here: see non-blocking finding 6 below (the frontmatter `description:` line).

### Also verify (items 1–6)

1. **Lane disjointness** — verified programmatically in both directions (extracted every file from every "File scope:" line plus the Lanes table into a script; zero duplicates across 38 distinct files/lanes). The "Files deliberately in NO lane's scope" table was cross-checked against the same list — no overlap. One real cross-lane dependency gap found (see non-blocking finding 1) and one internally-inconsistent citation about which subphase owns a conditional new suite (see non-blocking finding 2) — neither is a file-scope collision, both are missing/incorrect *edges or citations*.
2. **`verify-run-scope.sh:107`'s fallback** — read `resolve-config.sh`'s live unresolvable-var error path (`err "cannot resolve $var (...)"`, exit 1) against 4.1's planned `--expect` hard-error (also exit 1). Confirmed: **both failure modes share the same exit status**, so 4.2's text claiming the distinction can be made on "exit status and message" overclaims — only the message text differs. The distinction is still implementable and the plan does require a dedicated test case for it (4.2 criterion 5, 4.3 criterion 3), so this is not a design hole, just an imprecise sentence (finding 5).
3. **`document-confluence` rewrite judgment** — read the live 63-line `tool-based/confluence/skills/document-confluence/SKILL.md` in full and checked the plan's stage-by-stage table line-by-line, including every cited line number (`:3, :17, :18, :29, :30, :54`). All citations are accurate, and the "3 survive / 2 die / 2 reshape" count is correct against the actual file. This is a well-evidenced judgment, not over-reach — the surviving stages (connect-preflight, Drive offload, verify) are genuinely trigger-agnostic, and the dying/reshaping stages are genuinely tied to the run-shape taxonomy that no longer exists.
4. **R13 / drift coverage** — verified `docs/architecture.md:148-149`'s "No CI... 8 contract-test suites" text and `:64`'s six-layer list (missing `medusa`) against the live files, both exactly as cited. Confirmed by grep that exactly four files in the repo enumerate a top-level tree (`AGENTS.md`, `README.md`, `docs/architecture.md`, plus `docs/tool-based.md`'s separate tech-layer-structure tree) and all four are covered between 9.2 and 9.3's scopes for the two new directories this run adds. Coverage is adequate; R13's honesty about not being able to cheaply diff prose tree blocks is appropriate rather than evasive.
5. **A10 / CI degrade-vs-fail** — reasonable to leave as a builder judgment call given the plan's own fail-loudly-never-silently-degrade principle is already established elsewhere (2.3, 5.2(c)) and could be cross-referenced but doesn't strictly need to be re-derived here; low stakes since it only bites a project that installs the Confluence layer without the orchestrator hooks.
6. **Convention 16 / `find tool-based -type l`** — reproduced: currently returns nothing (confirmed empty, exit 0), so the baseline claim is true. Read `source-push-sync.md` to confirm `tool-based/<tech>/` content installs by copy into consuming projects' own trees, which is exactly the mechanism that would turn a relative symlink under `tool-based/confluence/ci/` into a dangling-or-wrong-target hazard in a different repo. The finding and its resolution (prose reference instead of a third symlink) are sound.

### Findings

- [non-blocking] **Missing cross-lane `after:` edge: 5.2 and 5.4 (lane 5) cite `resolve-config.sh --expect path|publish-target`, an interface subphase 4.1 (lane 4) creates, without a declared `after: 4.1` edge anywhere in the syllabus or the "Cross-lane edges" table.** This is a genuine internal inconsistency, not a guess: 5.4's own **Test oracle** line (body text, subphase 5.4) states outright that *"2.3's var split and 4.1's `--expect` semantics are the truth source,"* and 5.2's acceptance criterion 6 requires it to resolve both vars "with the matching `--expect."* The plan applies exactly this citation-dependency-declaration discipline everywhere else it has an analogous cross-lane citation (2.3 as the four-lane hub; 8.1 after 5.1; 8.2 after 2.1) — R6 even frames all of these as "citation rather than write dependencies." Omitting it here is inconsistent with that self-applied rule. Practical risk is low: the `--expect` flag's exact syntax is fully pinned in the plan text itself (not something a lane-5 builder would need to read lane 4's shipped file to learn), and both 5.2's and 5.4's relevant acceptance criteria are explicitly verified by *reading*, not by executing `resolve-config.sh` — so a parallel dispatch of lanes 4 and 5 would not produce a functional failure. Recommend adding `after: 4.1` to 5.2 and 5.4 (or to lane 5 generally) purely for consistency and to guard against a future change to the flag's exact spelling landing unnoticed in one lane and not the other.
- [non-blocking] **Three cross-references misattribute the conditional `tests/verify-run-scope.test.sh` decision to subphase 4.2 instead of 4.3.** Lines ~1172 (9.3's acceptance criterion 7: *"whatever 4.2 and 7.2 concluded they needed"*), ~1188 (10.1 item 1: *"any suite 4.2 or 7.2 added"*), and ~1327 (R4: *"4.2 and 7.2 conditionally"*) all name 4.2. But 4.2's own file scope (line 526) is `orchestrators/hooks/verify-run-scope.sh` only, with no test-file authority; the conditional `tests/verify-run-scope.test.sh` decision belongs to 4.3 per its own file-scope declaration (line 566: *"only if this subphase concludes no suite covers `verify-run-scope.sh`"*). Verified by re-reading both subphases' file-scope lines directly. Low practical impact — the operative instruction in each of the three places is "count the directory at edit time," not "trust this citation" — but worth a clean sweep of `4.2` → `4.3` in those three spots.
- [non-blocking] **A dangling convention citation.** Line 491 reads *"The interface — additive, stdout-compatible by construction (**convention 13**)."* Convention 13, as actually numbered in the "Conventions to enforce" list (line 129), is *"`CLAUDE_DOCS_DIR` is a path. Always"* — unrelated to additive/backward-compatible interface design. Read the full 16-item list; none of them states an additive-interface principle, so this citation appears to be an orphaned reference from an earlier draft's numbering, not a match to anything currently in the list.
- [non-blocking] **The "Conventions to enforce" list is numbered out of sequence in the raw source.** Items appear in the literal order …12, 13, 15, 16, 14 (lines 129–132) rather than 13, 14, 15, 16 — confirmed by direct read, not a copy artifact. Cosmetic for a rendered list, but a raw-source reader (including this gate, and anyone grepping "convention N") sees the literal digits out of order.
- [non-blocking] **4.2's fallback-distinction language overclaims "exit status."** 4.2 says to *"implement the distinction on the resolver's exit status and message"* between an unresolvable `CLAUDE_DOCS_DIR` and one rejected by `--expect`, but per 4.1's own spec both cases exit 1 (verified against the live `resolve-config.sh`'s current `err()`-based unresolvable path, and against 4.1's own text: *"An unresolvable var still exits 1 as today"* alongside *"...hard error, exit 1"* for `--expect` rejections). Only the stderr message text differs. Still implementable — the two message shapes are textually distinct today (`"cannot resolve $var (...)"` vs. the planned `"CLAUDE_DOCS_DIR must be a local path; ..."`) — but the plan should say "message only," not "exit status and message," so a builder doesn't go looking for a distinguishing exit code that 4.1 never provides.
- [non-blocking] **`artifact-locations.md`'s frontmatter `description:` line is not covered by 2.3's acceptance criteria.** The live file's frontmatter reads (verified, line 2): *"...`CLAUDE_DOCS_DIR` may also name a Confluence location"* — the same stale/inverted claim 2.3's body-content criterion 7 targets via `grep 'becomes the source of truth'`, but that grep would not catch this line (different wording, different location — frontmatter vs. body). None of 2.3's eleven acceptance criteria explicitly requires updating the frontmatter description, so a builder following the criteria literally could leave it stale.
- [non-blocking] **5.4 is absent from the Skill Mapping table's "existing implementation... contract-tester only, no coder" enumeration** (line ~1368), even though 5.4's own detail block explicitly declares exactly that oracle (line 762: *"Contract-tester only, no coder"*). Minor omission in the summary roundup table only; 5.4's own detail block is unambiguous.
- [non-blocking] **`orchestrators/skills/dae/build.md` is listed twice in the "Files deliberately in NO lane's scope" table** (lines ~1272 and ~1279, the second marked "— revisited"). Redundant, not conflicting — both entries correctly exclude it from any lane, just via two rows instead of one.

### Open questions

(none — every item above was independently verified against the live tree, the plan's own cross-references, or an external source, and none rises to a blocking classification; nothing here requires a user decision to resolve, only a text pass)
