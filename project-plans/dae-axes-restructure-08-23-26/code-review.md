# code-review

## Round 1 — 08-25-26

```
verdict: ready
next: proceed
blocking: 0
non-blocking: 2
```

### Findings

- [non-blocking] `orchestrators/skills/dae/report.md:14` — *"`--against` is `forbid` on **both rows**: this middle NEVER resolves anchors and never loads `resolve-anchor`."* This is the one sentence in the file that keys on row **cardinality** rather than a resolved axis **value**, contradicting the file's own governing principle at `:3` (*"Every branch below is on a resolved axis VALUE … never on the type name — that is the only thing keeping the two presets from drifting apart"*). Verified accurate today — `workflows.yaml`'s two `pipeline: report` rows (`map`, `analyze`) are both `against: forbid` (confirmed by direct read of `workflows.yaml:22-23`) — and confirmed non-fatal: the actual enforcement is `dae/SKILL.md`'s seam table (`SKILL.md:100`) plus the `AGAINST_COUNT`-keyed load rule (`SKILL.md:84`), both axis-value-keyed, neither reading `report.md:14`. Independently reproduced 10.2's own finding (raised by lane 10, routed as a non-blocking scope gap rather than absorbed, since `report.md` was 6.1's merged file scope, not 10.2's). A future third `pipeline: report` row with `against: optional` (exactly the shape 10.2's own throwaway `audit` row used, reverted before merge) would make the clause read false while the router kept working correctly — a latent doc-staleness risk, not a functional defect. Recommend rewording to something like *"`--against` is `forbid` on every row of this pipeline today (verify the type's row rather than assuming); this middle never resolves anchors on its own — the router does, per its seam table"* at the next opportunity that touches this file. Does not block: no incorrect behavior results from it as written, and it was already surfaced and triaged by the run itself.

- [non-blocking] Structural observation carried forward from lane 9's exit report, not a finding against this diff (explicitly out of scope for this plan per the review brief): the contract-tester breach during Phase 9 (see Open questions) exposed that wave-1 packet dispatch is concurrent, so a blind tester can finish after its sibling coder's implementation has already landed in the same tree, making blindness a matter of prompt discipline in that window rather than an architectural guarantee. Worth a `builder.md` design follow-up (e.g., staggering tester spawn ahead of coder spawn, or a filesystem-level read-block for testers) in a future plan.

### Open questions

None blocking a verdict. For the record, two items investigated in depth and resolved during this review, documented here so a later reader does not re-investigate them:

1. **The Phase 9 contract-tester blindness breach** (`reports/l9-exit.md`, "Process findings the gates should see" #1): a blind tester for packet A ran one targeted `grep` against the implementation file while debugging its own failing assertions, disclosed this unprompted, and both affected assertions were rewritten from scratch by a second, uncontaminated tester working from the contract plus the lane's diagnosis (no implementation source). Verified adequate: (a) both rewritten assertions — the publisher-exclusivity check and the `--ref` retirement check — were independently re-run against the current tree via `bash .artifacts/checks/l9-9.1.sh <parent-worktree>`, producing `passed: 213 failed: 0`, confirming they exercise real content rather than a tautology; (b) reading the assertions' source shows they test for the presence of *semantic terms* (`only`/`sole`/`no other`/`single publisher`/`no direct push`) and *structural facts* (no `--ref` table row, `--ref` still named in retirement prose), not a copy of the coder's exact wording, which is the right shape for a contamination-safe rewrite; (c) no assertion was weakened per the report's own account (the publisher check still fails an implementation dropping the invariant; the `--ref` check went from one assertion to three). Containment judged adequate.

2. **Lane 2's identical coder/tester drift** (`reports/l2-exit.md`, "Ordering defect"): the `--rigor explore:high,med` argv-vs-algorithm-step ordering bug was present in both the shipped implementation and the packet's own test assertion (the test was corrected from the plan's normative worked example, then the implementation corrected to match), so the blind contract suite could not have caught it — only lane 2's own chunk-level read-through against the plan did. Re-verified directly: `orchestrators/hooks/resolve-type.sh feature --rigor explore:high,med` now returns `RIGOR_EXPLORE=high` with `RIGOR_PLAN=RIGOR_CODE=RIGOR_PR=med`, matching the plan's algorithm-step-order spec. Searched the other nine lanes' exit reports for a similar unrecovered instance of coder/tester co-drift (a defect present in both the implementation and its own test, uncaught by any suite): none found — every other "found and fixed" defect recorded across the exit reports (l1's read-naively-indistinguishable fix, l3's dropped-`status`/even-panel fixes, l4's restated-layout/retired-claim fixes, l9's three contract-criteria corrections) was caught by either the blind suite or the lane's own read-through, not shipped uncaught. No further finding.
