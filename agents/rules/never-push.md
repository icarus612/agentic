---
name: never-push
description: Git work is add + commit only; never push to a remote.
type: rule
domain: universal
---

# Never push
Git work is `git add` + `git commit` only. Never push to a remote.
