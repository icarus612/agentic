---
name: map
description: DEPRECATED alias — documentation-only runs are now the dae orchestrator's document workflow. /map routes straight to /dae --type document. This stub exists for one release of muscle memory, then disappears.
domain: universal
rules: []
model: haiku
model-fallback: [gemini-pro]
---

# map (deprecated alias)

This entry point was consolidated: invoke the **`dae`** skill with `--type document`, passing any other arguments through unchanged. Do that now — read and follow the `dae` skill — and tell the user once, briefly, that `/map` is now `/dae --type document`.
